# AUTO-GENERATED by scripts/gen_lm_head_configs.py -- edit the generator, not this file.
# c4 eval of the b2_25 lm_head baseline
#
# lm_head baseline: b2_25
# Dense references (Qwen3-30B-A3B-Thinking-2507): HellaSwag 78.56, MMLU 80.91
# (stderr ~0.41-0.45 pt). Pre-registered success bar: HellaSwag >= 78.1 and
# MMLU >= 80.5 at <= INT4-equivalent head storage.

test_only: true

model_name_or_path: "Qwen/Qwen3-30B-A3B-Thinking-2507"
load_in_4bit: false
load_in_8bit: false
trust_remote_code: true

test_speed: false
real_slim: false
shrink_gate: false

device: "cuda"
dtype: "bf16"
use_wandb: true
wandb_project: "yequan26-a100-qwen3-30b-eval"
wandb_name: "lmhead_b2_25_c4"

num_workers: 0

eval_sample_limit: 500
eval_split: "test"
per_device_eval_batch_size: 4
batch_size: 16
num_fewshot: 0

prune_kwargs:
  prune_ratio: 0.0            # head-only arm: MoE path untouched
  lm_head:
    enabled: true
    method: "archead"
    rank: 10
    correction_rank: 6
    group: 64
    residual_bits: 4
    metric_power: 0.75
    ridge: 0.001
    activation_metric: true
    calib_dir: "./calib/lm_head_qwen3_30b_a3b"
    compute_device: "cpu"   # D x D eigh/SVD off the sharded GPUs
    diagnostics: true
    calib_kwargs:
      unigram_min_tokens: 5000000
      sigma_batches: 32
      sigma_batch_size: 8
      sigma_seq_len: 512

eval_task_names: "c4"
output_dir: ./results_eval/lmhead_b2_25_c4
scores_dir: ""

attn_implementation: "sdpa"
